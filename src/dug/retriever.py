"""Hybrid retriever — merges structural graph results with semantic search."""


def hybrid_search(embedder, graph, bug_input: str, top_k: int = 5) -> list[dict]:
    # Phase 3
    raise NotImplementedError("Phase 3")
