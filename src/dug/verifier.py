"""Verifier — confirms candidate files actually contain expected symbols via ripgrep."""


def verify_files(candidate_files: list[str], symbols: list[str]) -> list[str]:
    # Phase 3
    raise NotImplementedError("Phase 3")
