"""Prompt builder — assembles the Claude Code prompt using pure Python f-strings."""


def build_prompt(bug_input: str, ranked_files, git_commits, signals: dict) -> str:
    # Phase 3
    raise NotImplementedError("Phase 3")
